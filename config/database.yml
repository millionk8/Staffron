development: &default
  adapter: postgresql
  encoding: unicode
  database: api_development
  pool: 5
  username: postgres
  password:
  host: db

test:
  <<: *default
  database: api_test

production:
   url: <%= ENV['DATABASE_URL'] %>
